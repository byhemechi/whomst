apiVersion: v1
kind: Service
metadata:
  name: whomst
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "netzle.net,www.netzle.net,debug.netzle.net"
spec:
  selector:
    app: whomst
  ports:
    - port: 80
      targetPort: http
  type: LoadBalancer
